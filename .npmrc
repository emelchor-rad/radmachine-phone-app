# Expo ships peer-dependency conflicts it expects the installer to tolerate
# (jest-expo pulls a react-dom newer than the react the template pins).
# Without this, `npx expo install` fails with ERESOLVE.
legacy-peer-deps=true

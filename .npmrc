# Expo SDK 57 ships peer-dependency conflicts it expects the installer to
# tolerate (jest-expo pulls react-dom 19.2.8 while the template pins react
# 19.2.3). Without this, every later `npx expo install` fails with ERESOLVE.
legacy-peer-deps=true
